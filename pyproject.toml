[project]
name = "encaminhador"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11.7",
    "pytelegrambotapi>=4.24.0",
    "python-decouple>=3.8",
]
